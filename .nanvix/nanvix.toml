[package]
name = "libxslt"
version = "1.1.42"
nanvix-version = "0.16.75"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["256mb"]

[dependencies]
libxml2 = "2.12.9"
zlib = "1.3.1"
